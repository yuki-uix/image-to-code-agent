---
description: Convert a UI screenshot into html, react, vue, or structured design-system artifacts using the repo image-to-code skill.
argument-hint: <image-path> [--framework html|react|vue] [--mode simple|structured] [--design-system path] [--out path]
---

Use the project skill at `skills/image-to-code/SKILL.md`.

Forward these arguments exactly:

```txt
$ARGUMENTS
```

If no arguments were provided, ask for the screenshot path and desired framework or mode.

