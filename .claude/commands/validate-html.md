---
description: Validate and optionally repair generated self-contained HTML using the repo validate-html skill.
argument-hint: <html-path> [--fix]
---

Use the project skill at `skills/validate-html/SKILL.md`.

Forward these arguments exactly:

```txt
$ARGUMENTS
```

If no HTML path was provided, ask for it.

