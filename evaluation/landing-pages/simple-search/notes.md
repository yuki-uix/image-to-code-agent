# Simple search landing page

This synthetic Golden Case isolates three MVP behaviors: region detection, extraction of repeated tags, and generation from an explicit component tree.

The screenshot is SVG so the repository can keep a readable, deterministic fixture. Production inputs may be PNG, JPEG, or WebP.
